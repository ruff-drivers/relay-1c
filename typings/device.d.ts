export declare class Relay extends RuffDevice {
    /**
     * Turn on the relay.
     */
    turnOn(): void;

    /**
     * Turn off the relay.
     */
    turnOff(): void;
}

export default Relay;
